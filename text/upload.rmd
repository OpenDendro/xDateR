Upload (drag or browse) a standard file of ring widths (.rwl) readable by `read.rwl` (e.g., a tucson/decadal file). Standard descriptive statistics and a plot are produced. A report can be generated at the bottom of the page.
